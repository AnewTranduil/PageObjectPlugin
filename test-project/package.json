{
  "name": "page-mirror-test-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "postinstall": "npx playwright install chromium || true"
  },
  "devDependencies": {
    "@playwright/test": "1.59.0",
    "@types/node": "^24.12.0",
    "playwright-snapshot-saver": "file:../packages/snapshot-saver",
    "serve": "^14.2.0"
  }
}
